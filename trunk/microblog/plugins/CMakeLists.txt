PROJECT(microblog)

FIND_PACKAGE(Qt4 REQUIRED)

cmake_minimum_required(VERSION 2.6)

ADD_DEFINITIONS (${QT_DEFINITIONS})

INCLUDE_DIRECTORIES (
    ${CMAKE_SOURCE_DIR}
    ${CMAKE_BINARY_DIR}
    ${QT_INCLUDE_DIR}
)
